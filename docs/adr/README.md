# Architecture Decision Records (ADR)

ADR — это запись одного значимого архитектурного решения. Правила ведения:

- **Один immutable файл на решение.** Каждое решение — отдельный файл.
- **Сквозная нумерация** `000N` (`0001`, `0002`, …) в порядке принятия.
- **Не редактируются после `accepted`.** Если решение отменяется или заменяется,
  создаётся НОВЫЙ ADR с новым номером, а старому проставляется статус `superseded`
  (со ссылкой на заменяющий ADR). Так сохраняется история решений «как было».

Возможные статусы: `proposed`, `accepted`, `superseded`, `deprecated`.

## Индекс

| # | Решение | Статус | Файл |
|---|---------|--------|------|
| 0001 | payment_method_id vs external_id — источник истины для токена ЮKassa | accepted | 0001-payment-method-id-vs-external-id.md |
| 0002 | get_users_emails — внутренний admin-гейт вместо REVOKE от authenticated (N15) | accepted | 0002-get-users-emails-internal-admin-gate.md |
| 0003 | renewal idempotency-guard — сверка GET /v3/payments до создания платежа автопродления (N10) | accepted | 0003-renewal-idempotency-guard.md |
| 0004 | rate limiting — table-based счётчик в Postgres на публичных эндпоинтах (N13) | accepted | 0004-rate-limiting-table-based.md |
| 0005 | shared workspaces — роли/инвайты по TF-ID + `workspace_id` text→uuid + FK + ON DELETE CASCADE (Wave B) | accepted | 0005-shared-workspaces.md |
| 0006 | F12 (P4): admin-список пользователей — SECURITY DEFINER RPC `get_admin_users_summary` с admin-гейтом вместо GRANT на view; view дополнен `public_user_id` | accepted | 0006-admin-users-list-security-definer-rpc.md |
| 0007 | F13: краш открытия задачи в shared (стабильная `EMPTY_RECORDS` в zustand-селекторе) + FK `client_id` (push всегда текущее устройство) + дедуп осиротевших personal-ws | accepted | 0007-workspace-crash-clientid-dedup.md |
| 0008 | F14: полный pull членства в двух скоупах (`user_id` + `workspace_id`) — участники shared видны, ws восстанавливаются при рестарте; refresh сайдбара при leave | accepted | 0008-workspace-members-full-pull-leave-refresh.md |
| 0009 | F15: `accept_invite` — `INSERT ... ON CONFLICT (workspace_id, user_id) DO UPDATE` (реактивация soft-deleted членства после leave, стабильный uuid) | accepted | 0009-accept-invite-upsert-membership.md |
| 0010 | F16: авто-обнаружение и восстановление битой локальной SQLite при старте (`detectAndRecoverCorruption`, `SqliteCorruptError`/`withCorruptionGuard`, prune-skip, тост + reload) | accepted | 0010-sqlite-corruption-auto-recovery.md |
| 0011 | F17: реконсиль uuid членства при рассинхроне local↔server (fallback-матчинг `applyCloudRowMembers` по `(workspace_id, user_id)` + переклейка uuid; откат F16-эскалации 2067→corruption) | accepted | 0011-membership-uuid-mismatch-reconcile.md |
| 0012 | F18: ГИДРАЦИЯ `workspaces` + `workspace_members` из нативной data.db в webDb-зеркало при старте (Tauri) — **истинный корень** «shared-ws пропадают после рестарта»; зеркало стартовало пустым по членству → INSERT → 2067. F14–F17 чинили симптом | accepted | 0012-tauri-hydrate-workspaces-members.md |
| 0013 | F19: дубли сид-статусов — недетерминированная идентичность сида (новый `uuidv7` на каждый прогон) при дедупе пулла только по `uuid`; детерминированное локальное схлопывание `dedupeSeedStatuses()` + tombstone-фолбэк резолвера + реентерабельность `initDb()`. Плюс добивка гидрации зеркала: `workspace_settings`, `task_activity_log` | accepted | 0013-seed-identity-nondeterminism-and-mirror-hydration-gap.md |
| 0014 | F21: локальное per-account хранилище для free-аккаунтов — постоянный слот `localStorage['taskflow.localstore.v1.<userId>']` поверх `buildBackup`/`applyBackup`, сохранение данных уходящего аккаунта и автовосстановление при возврате + автосейв активной free-сессии; только free-ветка, pro/облако не затронуты, без серверных миграций | accepted | 0014-free-tier-local-account-store.md |
| 0021 | F28: слот free-аккаунта (ADR 0014) становится workspace-aware — `buildBackup`/`applyBackup` сохраняют `workspaces`/`workspace_members` и исходный `workspace_id` каждой строки вместо штамповки единым `importWsId` — второе личное пространство больше не теряет owner-membership и не гасится `dedupePersonalWorkspaces`; обратно совместимо с легаси-бэкапами, без серверных миграций. Номера 0015–0020 зарезервированы под откатанные патчи F22–F27 (чинили симптомы в reconcile/dedupe, не корень) | accepted | 0021-free-slot-workspace-aware.md |
| 0022 | F29: `applyBackup` (workspace-aware режим) нормализует legacy/сиротский `workspace_id` строк (напр. `ws_local`, созданный до переклейки `ws_local→ws_<uid>`), которого нет среди восстановленных `payload.workspaces` — переназначает на канонический personal (settings-указатель, иначе personal с наименьшим `sort_order`); валидные `workspace_id` (включая второе личное) не трогаются; легаси-бэкапы без `payload.workspaces` не затронуты | accepted | 0022-restore-legacy-ws-normalize.md |
| 0023 | F30: файловый снимок (`snapshots.ts`) становится fallback-источником авто-restore free-аккаунта при пустом localStorage-слоте (ADR 0014) — последний непустой `before_account_switch`-снимок для входящего аккаунта восстанавливается через уже существующий `restoreSnapshot`; при `needsRestart:true` (Tauri) переиспользуется механизм перезапуска из `Settings.tsx`; пустые снимки (`taskCount=0`/`size=4096`) не восстанавливаются; пересматривает альтернативу C из ADR 0014; Pro/Trial и web-режим (без перезапуска) не затронуты | accepted | 0023-free-restore-from-file-snapshot.md |
| 0024 | F31: смена аккаунта оставляла in-memory `currentWorkspaceId` залипшим от предыдущего аккаунта (ws-scoped фильтр прятал верные задачи при верном счётчике) — логика выбора current workspace из работающего `init()` вынесена в helper `hydrateCurrentWorkspaceId` и переиспользуется в `reloadAccountBinding()` после `loadWorkspaces()`; слот/снимок free-аккаунта (F28–F30) и Pro/Trial не затронуты; тесты зелёные, ждёт ручную проверку | accepted | 0024-hydrate-current-workspace-on-account-switch.md |
| 0025 | F32: `applyBackup` перепривязывал `task.status_id`/`tag_id` по имени БЕЗ учёта `workspace_id` — при 2+ пространствах с одноимёнными сид-статусами задача получала status_id чужого ws → невидима на доске при верном счётчике — ключ мапы стал вида `${workspace_id}|${name}` (fallback на старую по-имени мапу для легаси-бэкапов) + новая идемпотентная `repairTaskStatusWorkspaceMismatch()` чинит уже битые на диске данные в `init()`; слот, `AccountSwitchGate.tsx`, F30/F31, схема/миграции не затронуты; тесты зелёные, ждёт ручную проверку | accepted | 0025-restore-status-remap-workspace-aware.md |
| 0026 | F33: `applyBackup` в replace-режиме падал на UNIQUE(`workspaces.uuid`) и (`workspace_members.workspace_id,user_id`), когда personal-пространство уже пересоздано `reconcilePersonalWorkspace` ДО restore → исключение → аварийный откат на файловый снимок (F30) терял второе пространство — `INSERT INTO workspaces` → `INSERT OR REPLACE`, `INSERT INTO workspace_members` → `INSERT OR IGNORE` (связи идут по uuid-строке, подмена numeric id безопасна); доказан на реальной `data-4.db`+логе; схема/миграции, `localAccountStore.ts`, F30/F31/F32 не тронуты; тесты зелёные, ждёт ручную проверку | accepted | 0026-applybackup-idempotent-workspaces-members.md |
| 0027 | Миграции на прод применяются ВРУЧНУЮ (Management API / MCP `apply_migration` / Dashboard) с `confirm_action`; CI их НЕ применяет — прежнее утверждение в `docs/migrations.md` было ошибочным (ни один workflow не обращается к прод-БД, `db-tests.yml` работает на эфемерном Postgres). Мерж `feat/workspaces → main` — merge-коммитом, без изменений прод-БД (0027–0040 уже применены); прод легально опережает `main` | accepted | 0027-manual-migrations-and-workspaces-merge-to-main.md |
| 0028 | F36: сид-справочник штампуется ЯВНО переданным `workspace_id` — `ensureSeededIfEmpty(seedWsId?)` / `ensureWelcomeTaskIfNeeded(userId?, seedWsId?)` берут ws-id из аргумента (`computeWorkspaceId(userId)` из `AccountSwitchGate` и обоих bootstrap-мест оркестратора) вместо чтения указателя из БД, куда в Tauri `db.run()` доводит запись fire-and-forget → на первом входе в свежий free-аккаунт 7 сид-статусов уезжали на `ws_local`, а welcome-задача на `ws_<uid>` (доска без колонок, «Задачи» пустые); статус для welcome ищется строго внутри целевого ws; второй контур — `reconcileLocalPlaceholder` считает `ws_local` присутствующим и по дочерним таблицам (`LOCAL_WS_CHILD_TABLES`) и переклеивает сирот, лечя уже испорченные базы; членство/инвайты, pro-путь, слот, снимки и схема не затронуты, новых миграций нет | accepted | 0028-seed-workspace-id-explicit.md |
| 0029 | F37: записи в нативную SQLite (Tauri) идут ОДНОЙ последовательной очередью (`nativeWriteChain`/`enqueueNativeWrite`) вместо независимого промиса на команду — гарантирован порядок применения (раньше быстрый DELETE мог лечь после медленного INSERT: зеркало и файл расходились) — плюс новая точка ожидания `flushNativeWrites()`, которую ждут `createSnapshot`, `restoreSnapshot` и выход из аккаунта; новый `logoutLocalSlot.saveFreeSlotBeforeLogout()` пишет слот free-аккаунта прямо перед обычным logout (раньше слот обновлялся только через 30-секундный дебаунс автосейва, `beforeunload` или при входе в другой аккаунт), из-за чего задача, созданная за секунды до выхода, могла пропасть при перезапуске приложения; синхронный контракт `db.run()`, инвариант «слот только для free» (ADR 0014), облачный путь Pro/Trial и схема БД не затронуты | accepted | 0029-native-write-queue-and-logout-slot.md |
| 0030 | F38/F39 (навигация, пункты 3 и 9 брифа): активная вкладка приводится к «Вкладке по умолчанию» ОДНОЙ точкой — хук `useDefaultTabNavigation` в `App` реагирует на смену пользователя и смену пространства (вместо дублирования `navigate` в пяти местах смены ws и вместо навигации внутри стора); секция «Настроек» читается из `useLocation().hash` через новый `parseSettingsSection`, а прежняя проверка `window.location.hash === '#subscription'` вместе со слушателем `hashchange` удалена — под HashRouter полный хэш содержит ВЕСЬ маршрут (`#/settings#subscription`), поэтому переходы в «Подписку»/«Обновления» не работали никогда; тост обновления ведёт на `/settings#updates`, возврат с оплаты — на `/settings#subscription`, `<a href="/settings">` в Checkout заменён навигацией роутером. Роутер (HashRouter), `switchWorkspace()`, логика членства и схема БД не затронуты | accepted | 0030-default-tab-navigation-and-settings-hash-sections.md |
| 0031 | F40 (пункт 5 брифа): во вкладке «Участники» вместо имени показывался кусок внутреннего uuid (`user_id.slice(0,8)`), а аватары выглядели одинаковыми — корень не в UI, а в own-row RLS на `profiles` (`auth.uid()=id`), из-за которой чужой ник/TF-id/аватар клиенту недоступны в принципе. Вместо ослабления RLS (политика открыла бы строку целиком, включая `email`) — новая SECURITY DEFINER STABLE RPC `get_workspace_member_profiles(p_workspace_id)` (миграция `0043`) с проверкой членства вызывающего через `has_workspace_role(...,'viewer')`, исключением вышедших (`deleted_at`) и публичным минимумом в сигнатуре (`user_id`, `public_user_id`, `nickname`, `avatar_variant`, `avatar_color`, `bio`; email отсутствует физически, проверено pgTAP). Клиент: `src/lib/memberProfiles.ts` (RPC + кэш `localStorage['tf.memberProfiles.v1']` + правило «ник, иначе TF-id»), карточка участника `MemberInfoModal` по клику на имя, реальные аватары в списке; в журнале активности фолбэк «кусок uuid» заменён на кэш профилей и нейтральное «Участник». Логика членства/приглашений (ADR 0008/0009/0011), RLS `profiles` и sync-цикл НЕ изменены | accepted | 0031-workspace-member-public-profiles-rpc.md |
| 0032 | F41 (пункт 6 брифа): цвет аватара становится ЯВНЫМ свойством профиля — `profiles.avatar_color` (миграция `0042`, nullable + CHECK `^#[0-9a-f]{6}$`) вместо покраски глифа классами темы (`text-accent`/`bg-surface-alt`), из-за которой цвет менялся вместе с темой приложения и у всех участников выглядел одинаковым; `NULL` = цвет не задан → прежнее поведение (обратная совместимость). `Avatar` красится inline через `avatarTint()` на ручном `rgba()` (без CSS `color-mix` — системный WebView в Tauri), невалидное значение игнорируется. Выбор формы и цвета вынесен в модалку `AvatarEditorModal` (превью на светлой и тёмной подложке, системный пикер + hex-поле + 10 свотчей; «Отмена» ничего не меняет), редизайн «Настройки → Аккаунт»: блок «Профиль» (ник слева, аватар и «Изменить» справа, ниже «О себе», затем «Ваш ID») перенесён ВЫШЕ блока email/«Зарегистрирован»/«Последний вход». Presence передаёт `avatar_color` опциональным полем meta (клиент прошлой версии просто не пришлёт цвет) | accepted | 0032-explicit-avatar-color-and-account-redesign.md |
| 0033 | F42 («покинуть пространство» не доезжало до сервера): клиентский push отправлял `op='delete'` тем же PostgREST-upsert'ом (`INSERT ... ON CONFLICT DO UPDATE`), а Postgres проверяет INSERT-политику RLS даже в ветке `DO UPDATE` — на `sync_workspace_members` INSERT разрешён только владельцу, поэтому у editor'а гашение падало с 42501 (permanent → строка молча оседала в outbox: на сервере членство оставалось живым, у владельца участник висел в списке, а полный pull фазы 1 возвращал пространство вышедшему). Корень доказан прод-пробой под ROLLBACK от имени пострадавшего пользователя: upsert-путь → 42501, `UPDATE ... SET deleted_at` → OK (политика `sync_workspace_members_self_leave_update` уже существовала). Фикс без миграций и без изменения RLS: `op='delete'` уходит `UPDATE`'ом по ключу спека (`id`, для `workspace_settings` — `workspace_id,key`) узким патчем `deleted_at/updated_at/version/client_id`, построчно; 0 затронутых строк = успех (гасить нечего). Путь `op='upsert'`, `PUSH_ORDER`, backoff, логика членства/приглашений (ADR 0008/0009/0011) и схема БД не изменены; альтернативы «дать editor'у INSERT в RLS» и «отдельная RPC `leave_workspace()`» отклонены | accepted | 0033-soft-delete-push-via-update.md |
| 0034 | F44 (§7.39): экран `/workspace-settings` доступен ВСЕМ участникам пространства — гейт `{canEdit && …}` вокруг ссылки «Настройки пространства» в `WorkspaceSwitcher` снят, потому что он отрезал `viewer`'у единственный вход на экран, а вместе с ним и кнопку «Покинуть пространство» во вкладке «Участники»: наблюдатель оказывался заперт в пространстве до ручного удаления владельцем. Серверная модель прав это ограничение не поддерживала — политики прод-БД `sync_workspace_members_select_ws_role` (viewer читает список участников) и `sync_workspace_members_self_leave_update` (`user_id = auth.uid() AND role <> 'owner'`, ср. ADR 0033) разрешают наблюдателю и чтение, и самостоятельный выход. Ролевые ограничения остаются ВНУТРИ экрана (rename/delete — только owner, справочники — `useCanManageWorkspace()`, `MembersTab` для editor/viewer = список + «Покинуть пространство»), новый инвариант: каждая новая секция экрана гасится по роли сама. Отклонены отдельный урезанный экран для viewer'а и дублирующий пункт «Покинуть пространство» в переключателе. Плюс якорь `data-onboarding="workspace-switcher"` для подсветки шага «Пространства» в туре. RLS, схема БД и sync не затронуты, миграций нет | accepted | 0034-workspace-settings-visible-to-all-members.md |
| 0035 | F49 (§7.44): справочники статусов и тегов — ws-scoped данные, поэтому у них РОВНО один экран. Гипотеза «два пути в настройки пространства» не подтвердилась (ссылка на `/workspace-settings` одна, `WorkspaceSwitcher.tsx:167`), дублировался сам справочник: `TagsSection`/`StatusesSection` были объявлены в `src/pages/Settings.tsx`, рендерились секциями `/settings` И одновременно импортировались страницей `WorkspaceSettings.tsx` (импорт «страница → страница»). Секции `statuses`/`tags` удалены из левой навигации «Настроек», из типа `Sub` и из `SETTINGS_SECTIONS`; компоненты перенесены без функциональных изменений в новый `src/components/WorkspaceReferenceSections.tsx`, откуда их берёт экран настроек пространства. Ролевой гейт `useCanManageWorkspace()` (Bug #5) переехал вместе с кодом, модель прав не менялась (ср. ADR 0034). Отклонены: синхронизация двух точек входа, перенос справочников в общие настройки, редирект со старого хэша, минимальный диф с сохранением экспорта в `Settings.tsx`. Старые глубокие ссылки `/settings#statuses`/`#tags` теперь не распознаются и оставляют текущую секцию. RLS, схема БД и sync не затронуты, миграций нет | accepted | 0035-workspace-reference-sections-single-screen.md |
| 0036 | F56 (§7.51): карточка входящего приглашения в сайдбаре показывала нейтральное «Приглашение в общее пространство» без названия пространства и без автора. Корень — RLS, а не UI: приглашённый ещё не участник ws, поэтому `select name from sync_workspaces` и `select … from profiles` для него дают 0 строк (проверено на проде под ролью приглашённого), а в самой строке инвайта лежат только идентификаторы. Введена SECURITY DEFINER STABLE RPC `get_my_pending_invites()` (миграция 0044) без параметров, с гейтом `i.target_user_id = auth.uid()`: отдаёт мои pending-приглашения вместе с именем пространства и публичным минимумом профиля пригласившего (public_user_id/nickname/avatar_variant/avatar_color/bio), без email; гранты по ADR 0002. Клиент переключён на RPC, прежний прямой SELECT оставлен fallback'ом на случай, если миграция ещё не применена (ADR 0027). В UI — заголовок с названием ws и строка «От: …», клик открывает ту же MemberInfoModal, что и список участников (ADR 0031). Отклонены: ослабление RLS на sync_workspaces/profiles, денормализация имени в строку инвайта, отдача имени только через accept_invite, сохранение approach 5.b, отдельная RPC на каждое поле. Мутации инвайтов, членство и sync-цикл не тронуты | accepted | 0036-invite-card-workspace-name-and-inviter-profile.md |
